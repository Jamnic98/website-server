version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install typescript@latest -g
      - npm install
  build:
    commands:
      - tsc

artifacts:
  files:
    - package.json
    - package-lock.json
    - 'build/**/*'
  discard-paths: no
